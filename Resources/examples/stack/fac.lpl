// output factorial of the input

main(x) {
   printint fac(x);
   printchar 13; printchar 10;
}

fac(x) {
   if (x < 2) {
      return 1;
   } else {
      x = x - 1;
      return (x + 1) * fac(x);
   }
}
